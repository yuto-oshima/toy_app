<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<%= @user.microposts.each do |r| %>

  <p>
    <strong>MicropostId:</strong>
    <%= r.id %>
  </p>

  <p>
    <strong>Content:</strong>
    <%= r.content %>
  </p>

<% end %>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
